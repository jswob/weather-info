<MainWeather @weather={{this.selectedForecast}} @isCelsius={{this.isCelsius}} />
{{#each this.forecasts as |forecast|}}
  <SingleForecast @forecast={{forecast}} @selectedForecast={{this.selectedForecast}} @isCelsius={{this.isCelsius}} />
{{/each}}